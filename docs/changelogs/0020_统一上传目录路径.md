# 变更日志: 统一上传目录路径
**日期**: 2025-11-27 16:45
**任务**: 统一文件上传与静态访问的根路径，修复头像无法显示问题

## 📂 文件变更
### 修改 (Modified)
- `src/config/paths.ts`: 新增项目根目录、public、views、uploads 的统一路径常量。
- `src/server.ts`: 使用 `UPLOADS_DIR/PUBLIC_DIR/VIEWS_DIR`，并将 `/uploads` 静态目录指向与上传一致的路径。
- `src/controllers/uploadController.ts`: Multer 的 `diskStorage` 目标目录改为共享的 `UPLOADS_DIR`。

### 新增 (Created)
- `docs/changelogs/0020_统一上传目录路径.md`: 记录此次上传目录修复。

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 通过集中路径常量，保证 ts-node、编译产物及 Multer/静态服务三者的文件根路径一致；后续如需调整目录只需修改一处。

