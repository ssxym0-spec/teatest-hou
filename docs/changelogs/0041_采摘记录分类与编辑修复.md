# 变更日志: 采摘记录分类与编辑修复
**日期**: 2025-11-27 00:00
**任务**: 恢复采摘记录分类统计并修复编辑删除功能

## 📂 文件变更
### 修改 (Modified)
- `views/traceability-management.html`: 
  - 统一采摘记录与品类字段（ID、日期、权重、图片数组等），确保分类统计、日期回填与下拉框匹配。
  - `loadCategories` 结果归一化，`renderCategoryCards`/`renderHarvestRecords` 重新拾取对应 UUID，原有记录不再凭空消失。
  - `editHarvestRecord` 使用归一化数据，兼容 `harvestDate`/`harvest_date`、`mediaUrls`/`media_urls` 等多种字段，并正确展示队长头像，使编辑/删除不再抛出 `split` 错误。

### 新增 (Created)
- `docs/changelogs/0041_采摘记录分类与编辑修复.md`: 记录本次修复事项。

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 归一化逻辑复用在列表与详情接口之间，避免再出现 Mongo/Prisma 字段命名不一致导致的 UI 异常。
- 由于品类 `_id` 重新指向 Prisma UUID，前端的日期自动归类与统计恢复正常，未分类仅保留真实超范围记录。

