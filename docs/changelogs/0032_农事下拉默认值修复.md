# 变更日志: 农事下拉默认值修复
**日期**: 2025-11-27 21:25
**任务**: 确保没有农事活动时下拉框显示“无”

## 📂 文件变更
### 修改 (Modified)
- `views/growth-management.html`: 在表单回填时对 `farm-activity-type` 的取值做标准化，`NONE/null/undefined` 统一映射为中文“无”，并在选项列表不包含该值时强制回退到“无”，避免下拉框留白。

### 新增 (Created)
- 无

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 通过 `normalizedValue === 'NONE' ? '无' : normalizedValue` 和选项存在性检查，解决了后端返回英文枚举或空值时 UI 不显示默认项的问题。

