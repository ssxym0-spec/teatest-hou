# 变更日志: 修复生长日志前后端异常
**日期**: 2025-11-27 20:45
**任务**: 同步修复生长管理页面前端滚动错误与后端农事类型映射

## 📂 文件变更
### 修改 (Modified)
- `views/growth-management.html`: 更正详情页跳转时的滚动容器选择，避免因 `.form-content` 节点缺失导致点击日历后无法进入详情页。
- `src/controllers/growthController.ts`: 添加农事活动类型映射与清洗逻辑，确保提交的中文标签在写入 PostgreSQL 前转换成 Prisma 的枚举值，修复 `FarmActivityType` 校验错误。

### 新增 (Created)
- 无

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 通过 `normalizeFarmActivityType` 函数统一兼容带 emoji/空格的中文输入以及已是英文枚举的值，以防止 `Invalid value for argument 'farmActivityType'` 报错。
- 前端滚动定位改用 `.content-main` 容器并做空值保护，确保双页表单切换体验稳定。

