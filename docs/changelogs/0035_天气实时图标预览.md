# 变更日志: 天气实时图标预览
**日期**: 2025-11-27 00:10
**任务**: 在卡片信息天气选择框旁实时显示所选天气图标

## 📂 文件变更
### 修改 (Modified)
- `views/growth-management.html`: 新增天气图标路径规范化逻辑，并在天气选择器旁实时刷新预览图。

### 新增 (Created)
- 无

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 引入 `resolveWeatherIconUrl`，统一兼容不同存储格式（绝对地址、public/、weather/、uploads/ 等）。
- `loadWeatherOptions` 为每个选项写入标准化图标地址并缓存；`updateWeatherIcon` 支持从映射或选项 `dataset` 中取回图标，实现即时预览。

