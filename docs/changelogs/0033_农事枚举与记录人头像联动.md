# 变更日志: 农事枚举与记录人头像联动
**日期**: 2025-11-27 21:34
**任务**: 修复农事活动英文枚举无法回填与记录人切换头像不同步的问题

## 📂 文件变更
### 修改 (Modified)
- `views/growth-management.html`:
  - 新增 `FARM_ACTIVITY_ENUM_TO_LABEL` 与 `mapFarmActivityToDisplay`，在本地回填及下拉赋值时将 FERTILIZE/HARVEST 等英文枚举转换成对应中文标签，确保 UI 始终显示正确的农事类型。
  - 将“记录人”下拉绑定 `onchange="handleRecorderChange()"`，并在本地回填后刷新头像，实现选择器与头像的实时同步。

### 新增 (Created)
- 无

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 映射表覆盖英文与中文别名，保证无论数据来源如何都可得到规范显示值；头像逻辑复用既有 `handleRecorderChange`，避免重复代码。

