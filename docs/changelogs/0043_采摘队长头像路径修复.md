# 变更日志: 采摘队长头像路径修复
**日期**: 2025-11-27 00:00
**任务**: 复用 resolveAvatarUrl 修复采摘队长头像联动

## 📂 文件变更
### 修改 (Modified)
- `views/traceability-management.html`: 
  - 在 `loadCaptains` 中将头像保存到 `data-avatar-url`。
  - `updateCaptainAvatarFromSelect` 使用 `resolveAvatarUrl` 解析头像路径，保持与 0034 号记录人头像逻辑一致。

### 新增 (Created)
- `docs/changelogs/0043_采摘队长头像路径修复.md`: 记录本次头像路径归一化修复。

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 通过把原始 URL 封装成 `{ avatar_url: raw }` 交给 `resolveAvatarUrl`，复用既有的本地/相对路径处理逻辑，避免不同模块各自拼接上传目录。

