# 变更日志: 人员静态资源接入
**日期**: 2025-11-27 16:15
**任务**: 补齐人员头像等上传文件的静态服务

## 📂 文件变更
### 修改 (Modified)
- `src/server.ts`: 新增 `/uploads` 静态目录映射到 `public/uploads`，与旧版路径保持一致。

### 新增 (Created)
- `docs/changelogs/0017_人员静态资源接入.md`: 记录静态资源服务调整。

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 旧服务器通过 `app.use('/uploads', express.static(path.join(__dirname, 'public', 'uploads')))` 暴露头像等资源，现已在新服务器中等效实现，前端即可继续通过 `/uploads/**` 访问历史文件。

