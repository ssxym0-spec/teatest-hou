# 变更日志: 模板管理路由修复
**日期**: 2025-11-26 10:15
**任务**: 修复 `/admin/template-management` 访问到错误页面的问题，使其正确指向“模板管理中心”索引页，并为制作步骤模板详情页提供独立子路径。

## 📂 文件变更
### 修改 (Modified)
- `src/server.ts`: 
  - 新增显式路由 `GET /admin/template-management`，使用 `res.sendFile` 返回模板管理中心入口页 `template-hub.html`。
  - 新增路由 `GET /admin/template-management/steps`，用于展示原先的“制作步骤模板管理”详情页，对应 `template-management.html`。
  - 将通配路由从“处理 /admin/:page”调整为兜底路由，避免覆盖模板管理中心的专用路由。

### 新增 (Created)
- `docs/changelogs/0008_模板管理路由修复.md`: 记录本次模板管理页面路由调整的变更。

### 删除 (Deleted)
- 无

## 💡 技术说明 (Technical Notes)
- 由于原有的 `/admin/:page` 通配路由会将 `/admin/template-management` 映射到同名的 `template-management.html`，导致访问索引 URL 时误进入“制作步骤模板”详情页，因此需要在该通配路由之前插入更具体的路径匹配。
- 通过为“模板管理中心”索引页和“制作步骤模板”详情页分别定义 `/admin/template-management` 与 `/admin/template-management/steps` 两个路由，既保持了旧项目的 URL 语义，又避免了路由冲突。


