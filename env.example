DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
SESSION_SECRET="change-me"
# 生产环境基础URL：用于生成图片的完整URL
# 如果设置了此变量，将优先使用它；如果不设置，系统会自动从请求头推断
# 生产环境应填写真实域名，例如：https://hou.goodcat.ggff.net
BASE_URL=""
# 前端应用URL：用于CORS跨域资源共享配置
# 如果设置了此变量，将优先使用它；如果不设置，将使用默认值 https://tea.goodcat.ggff.net
# 生产环境应填写真实前端域名，例如：https://tea.goodcat.ggff.net
FRONTEND_URL=""
